replicaCount: 1

image:
  repository: docker.io/library/python-test-app
  pullPolicy: IfNotPresent
  tag: "v1"

service:
  type: NodePort
  port: 80
  targetPort: 5000
  nodePort: 30291

metrics:
  enabled: true
  path: /metrics
  port: 8000
